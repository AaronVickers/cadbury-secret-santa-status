{
  "name": "cadbury-secret-santa-status",
  "description": "Track the status of Cadbury Secret Santa links",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx tsc --build",
    "clean": "npx tsc --build --clean"
  },
  "devDependencies": {
    "@types/node": "^18.11.12",
    "@types/node-fetch": "^2.6.2",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "csv-parse": "^5.3.3",
    "node-fetch": "^2.6.7"
  }
}
